import React from 'react';
import { useDispatch, useSelector } from 'react-redux';
import { fetchData } from '../actions';

export default () => {
    return(
        <div>
            Dis your village, yo!
        </div>
    );
}